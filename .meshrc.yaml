sources:
  - name: UserAuth
    handler:
      grpc:
        endpoint: "192.168.1.130:50051"
        protoFile: "./proto/user.proto"
        packageName: "user"
        ServiceName: "UserService"
        useHTTPS: false

  - name: TeamMock
    handler:
      graphql:
        source: "./schemas/team.mock.graphql"
    transforms: []

  - name: GameMock
    handler:
      graphql:
        source: "./schemas/game.mock.graphql"
    transforms: []

  - name: TournamentMock
    handler:
      graphql:
        source: "./schemas/tournament.mock.graphql"
    transforms: []

serve:
  port: 4000
  mock: true
